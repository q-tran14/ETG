using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test_Character_Control : MonoBehaviour
{
    private Test_Character_Movement movement;

    // Start is called before the first frame update
    void Start()
    {
        movement = GetComponent<Test_Character_Movement>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
